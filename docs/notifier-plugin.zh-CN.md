# 通知扩展

内置通知渠道包括 Telegram、Email SMTP、Webhook、ntfy、Gotify、Bark、ServerChan、钉钉和飞书。

新增通知渠道时应遵守以下约定：

- 只接收已经规范化的告警或报告模型，不直接读取扫描器内部状态。
- 支持中文和英文消息模板，默认中文。
- 不在日志中打印 token、webhook、邮件密码或完整请求体。
- 网络错误应返回明确错误，不应导致 agent 崩溃。
- 发送超时、重试和消息长度应有边界，避免小内存 VPS 被异常通知拖垮。
